# איפיון: ליקוט לפי מיקום + שלב בדיקה (QC) + דוח חוסרים למזכירה

סוכם עם דניאל, 14.9.2026. **מומש ונבדק מקצה לקצה (14.9.2026)** — כולל תיקון קריטי נלווה: סנכרון Sigma חוזר היה מוחק בטעות התקדמות ליקוט/בדיקה (`sigmaIngest.js`). כתוסף לתוכנית המקורית: נבנה גם קישור ידני בין הזמנות (סעיף 10) ושיתוף מרוכז ללקוח במקום לכל שורה בנפרד (סעיף 5.1).

## 1. הבעיה והמטרה

היום ההזמנה נעלמת לתוך "ליקוט" כקופסה שחורה — מנוע הסטטוסים שלנו לא יודע כלום על מה קרה בפועל בתוך הליקוט (איזה פריט נמצא, איזה חסר), וברגע שהליקוט "הסתיים" אין בדיקה נוספת ואין דיווח מסודר על חוסרים למזכירה (היא צריכה לגלות את זה בעצמה כדי להוציא חשבונית נכונה).

המטרה: להוריד את המעקב לרמת **שורת פריט בודדת**, להוסיף **שלב בדיקה חובה** לפני אריזה, ולהפיק **דוח חוסרים ברור** (מק"ט + כמות) לסגירת חשבונית.

## 2. החלטות שסוכמו (לא לשנות בלי לחזור לדניאל)

| נושא | החלטה |
|---|---|
| מיקום פיזי | **קיים בסיגמא** — צריך רק לאתר את העמודה ולמשוך אותה דרך ה-Bridge |
| בדיקה (QC) | **חובה תמיד**, אי אפשר לדלג |
| מי בודק | **אין הגבלה טכנית** — יכול להיות אותו יוזר "מחסן" (יש login משותף אחד בפועל), הפרדת התפקידים היא הליך פנימי, לא נאכפת במערכת |
| דוח למזכירה | שורות חסרות בלבד, עם **מק"ט + כמות חסרה** |
| תיקון חשבונית | **ידני בשלב זה** — לא נשלח כלום בחזרה לסיגמא בשלב הזה |
| רעיון עתידי (לא עכשיו) | ייצוא קובץ TXT לסיגמא עם הכמויות בפועל, שיבצע UPDATE אוטומטי על ההזמנה. **דורש בירור נפרד** של פורמט הקובץ שסיגמא מצפה לו (יש למסמכי `06_erp_integration` תבנית import — צריך לבדוק אם יש מסמך מקביל ל"עדכון הזמנה קיימת", לא רק ליצירה) |

## 3. שינויי מודל נתונים (SQLite)

### `order_items_cache` — עמודות חדשות
```sql
ALTER TABLE order_items_cache ADD COLUMN qty_picked REAL;        -- כמות שנלקטה בפועל (NULL = עוד לא טופל)
ALTER TABLE order_items_cache ADD COLUMN pick_status TEXT;       -- 'picked' | 'partial' | 'missing'
ALTER TABLE order_items_cache ADD COLUMN pick_note TEXT;         -- הערת מלקט (אופציונלי, בעיקר לחוסר)
ALTER TABLE order_items_cache ADD COLUMN checked INTEGER DEFAULT 0;  -- 0/1 — האם הבודק אישר את השורה הזו
ALTER TABLE order_items_cache ADD COLUMN check_note TEXT;        -- הערת בודק אם תיקן משהו
-- location כבר קיימת כעמודה, כרגע תמיד NULL כי ה-Bridge לא ממלא אותה
```

### `workflow_events` — לא משתנה, ממשיך לתעד גם את המעברים החדשים כרגיל.

## 4. שינוי במנוע הסטטוסים

מוסיפים סטטוס חדש **`ready_for_check`** בין `picking` ל-`ready_to_pack`:

```
waiting_pick → picking → ready_for_check → ready_to_pack → waiting_pickup → delivered_to_ups/closed(איסוף עצמי) → closed
```

- `finishPicking` (הכפתור הקיים "סיום ליקוט") ישתנה: לפני שהוא מרשה לעבור הלאה, יבדוק שלכל שורות הפריט יש `pick_status` שאינו NULL (כל שורה טופלה — נלקטה/חלקית/חסרה). אם לא — שגיאה "יש שורות שלא טופלו".
- מעבר חדש: `POST /orders/:key/finish-check` — מ-`ready_for_check` ל-`ready_to_pack`. דורש שכל השורות עם `pick_status != 'missing'` יהיו `checked=1`. שורה שסומנה `missing` לא צריכה בדיקה (אין מה לבדוק).
- `OrderTimeline` (ציר הזמן בממשק) מקבל שלב נוסף: "בתור → ליקוט → בדיקה → אריזה → ...".

## 5. מסכי ממשק חדשים

### 5.1 מסך ליקוט משודרג (מחליף את מסך `picking` הקיים)
- שורות **ממוינות לפי `location`** (לא לפי `line_no` המקורי מסיגמא) — כדי שהמלקט ילך מסלול הגיוני.
- כל שורה: שם פריט, מק"ט, **תג מיקום גדול**, כמות מוזמנת.
- כפתור מהיר "✓ ליקטתי הכל" → `qty_picked = qty_ordered`, `pick_status='picked'`.
- אופציה לערוך כמות (חלקי) או לסמן "לא נמצא" (`pick_status='missing'`, אפשרות להוסיף הערה).
- מד התקדמות למעלה ("5/8 שורות טופלו").
- כפתור "סיום ליקוט" מופעל רק כשכל השורות טופלו → `finishPicking` → `ready_for_check`.
- **כפתור "שתף ללקוח" מרוכז (עודכן 14.9.2026 — לא לכל שורה בנפרד, כפתור אחד בסוף):**
  - מופיע ליד כפתור "סיום ליקוט", **רק** אם יש לפחות שורה אחת `partial`/`missing`, ורק אחרי שכל השורות טופלו.
  - בלחיצה אחת, האפליקציה מציירת **תמונה אחת** (canvas בצד הלקוח) עם **כל** השורות החלקיות/חסרות ביחד — לא הודעה נפרדת לכל פריט.
  - התמונה נשלחת לתפריט השיתוף המובנה של הטלפון (Web Share API עם קובץ) — WhatsApp/כל אפליקציה, בלי לצאת מהאפליקציה.
  - **גיבוי**: מכשיר שלא תומך בשיתוף קבצים — הורדת התמונה במקום זאת.
  - לא דורש שינוי בבקאנד — כל הלוגיקה בצד הלקוח (`shareShortage.js`).

### 5.2 מסך בדיקה (חדש, סטטוס `ready_for_check`)
- אותה רשימת שורות, מציג את מה שהמלקט דיווח (`qty_picked`, `pick_status`, `pick_note`).
- שורות `missing` מוצגות אך לא ניתנות לאישור (אין מה לבדוק בפריט שלא נמצא).
- לכל שורה אחרת: כפתור "✓ מאשר" (`checked=1`), או עריכה + הערה אם יש אי-התאמה (`check_note`).
- כפתור "אישרתי בדיקה — מוכן לאריזה" מופעל רק כשכל השורות הרלוונטיות `checked=1` → `finish-check` → `ready_to_pack`.

### 5.3 הרחבת "היסטוריה" — דוח חוסרים למזכירה
- בכל שורת הזמנה בהיסטוריה: אם יש שורת פריט עם `pick_status IN ('missing','partial')`, תג "⚠️ N פריטים חסרים".
- לחיצה על ההזמנה מציגה טבלה: מק"ט | שם פריט | כמות שהוזמנה | כמות שנלקטה | הערה.
- (אופציונלי בהמשך: מסך מרוכז "כל ההזמנות עם חוסרים" ברמת הארגון, לא רק per-order).

## 6. שינויים בבקאנד

- `backend/src/schema.sql` — 5 העמודות החדשות (סעיף 3).
- `backend/src/db.js` — `ensureColumn` migrations תואמות.
- `backend/src/workflow.js` — `ready_for_check` ב-`VALID_TRANSITIONS`, פונקציה `finishCheck`, ולידציה ב-`finishPicking`.
- `backend/src/routes.js` — endpoint חדש לעדכון שורת פריט בודדת (`POST /orders/:key/items/:lineNo/pick`, `POST /orders/:key/items/:lineNo/check`), ו-`POST /orders/:key/finish-check`.
- `backend/src/routes.js` (`/history`) — כולל את שורות הפריט עם `pick_status` כדי שהממשק יבנה את תג/טבלת החוסרים.

## 7. שינויים ב-Bridge (`bridge/sync.js`)

**שלב מקדים — עדיין לא בוצע:** להריץ סקריפט אבחון (כמו `inspect-schema.js` הקודמים) מול טבלת `azmanot` כדי לזהות באיזו עמודה שוכן המיקום הפיזי. לאחר זיהוי:
- להוסיף את העמודה לשאילתת שורות הפריט ב-`fetchOpenOrders()`.
- להעביר את הערך דרך `ingestOrders` אל `order_items_cache.location`.

## 8. תוכנית עבודה מוצעת (סדר בנייה)

| שלב | מה | תלוי ב-|
|---|---|---|
| 1 | סקריפט אבחון ל-Bridge — למצוא עמודת מיקום בסיגמא | גישה למחשב העבודה שלך |
| 2 | עדכון `sync.js` למשוך ולשלוח מיקום | שלב 1 |
| 3 | מיגרציית סכמה (5 עמודות חדשות) + `ensureColumn` | — |
| 4 | הרחבת מנוע הסטטוסים (`ready_for_check`, `finishCheck`) + endpoints לשורת פריט | שלב 3 |
| 5 | מסך ליקוט משודרג (מיון לפי מיקום, סימון per-line) | שלב 4 |
| 6 | מסך בדיקה חדש | שלב 4 |
| 7 | הרחבת ציר הזמן (OrderTimeline) לשלב הבדיקה | שלב 4 |
| 8 | הרחבת היסטוריה — תג + טבלת חוסרים | שלב 4 |
| 9 | בדיקה מקצה לקצה עם הזמנה אמיתית שיש בה חוסר מכוון | הכל |

**הערכת היקף:** בסדר גודל דומה (אולי מעט יותר גדול) מכל מה שנבנה עד כה לתור הליקוט הבסיסי — בעיקר בגלל המעקב ברמת שורה (לא רק רמת הזמנה) שדורש שינוי בכל המסכים שנוגעים בפריטים.

## 9. שאלות פתוחות להמשך (לא חוסמות את הבנייה הנוכחית)

- פורמט קובץ TXT עתידי לעדכון סיגמא (סעיף 2) — צריך מסמך spec נפרד מסיגמא/מהאפיון המקורי לפני שנתכנן את זה.
- האם דוח החוסרים צריך להיות "בר-ייצוא" (PDF/Excel) למזכירה, או מסך צפייה בתוך האפליקציה מספיק בשלב זה?

## 10. הזמנות מקושרות (נוסף ונבנה 14.9.2026)

**הבעיה:** לקוח שמוסיף פריטים בהזמנה נפרדת בסיגמא (יום-יומיים אחרי ההזמנה המקורית) — ההזמנה החדשה יורדת לסוף התור לפי זמן היצירה שלה, למרות שצריך לטפל בה יחד עם המקורית.

**החלטה:** קישור **ידני בלבד** — לא אוטומטי לפי אותו לקוח (לקוח קבוע יכול לגמרי לשים הזמנות לא-קשורות).

**איך זה עובד:**
- `workflow_state.linked_group_id` — הזמנות עם אותו group id "מקושרות".
- `POST /orders/:key/link` (body: `{otherOrderNum}`) — מזהה את ההזמנה השנייה לפי מספר, יוצר/משתף group id. זמין ל-agent (על הזמנה שלו) / warehouse / warehouse_manager / system_admin.
- `POST /orders/:key/unlink` — מסיר הזמנה בודדת מהקבוצה.
- **תור:** `queue.js` מחשב לכל הזמנה מקושרת את זמן היצירה **המוקדם ביותר בקבוצה** (בכל הסטטוסים, לא רק waiting_pick) ומשתמש בו למיון — כך ההזמנה החדשה "קופצת" למקום התור של הישנה. נבדק ומאומת: הזמנה שהייתה אחרונה בתור עלתה למקום מיד אחרי ההזמנה שאליה קושרה.
- **ממשק:** תג "🔗 מקושרת להזמנה X (סטטוס)" בכותרת ההזמנה + כפתור "קשר להזמנה אחרת"/"בטל קישור", זמין למחסן/סוכן/מנהל.
- **לא ממומש (במכוון, לשמור פשוט):** אין חסימה פיזית שמונעת משלוח של אחת בלי השנייה — רק תג ויזואלי ומיקום בתור.

### 10.1 חיפוש לפי שם לקוח (נוסף 14.9.2026)

דניאל ציין שלא תמיד זוכרים את מספר ההזמנה בזמן קישור. נוסף חיפוש חי בחלונית "קשר להזמנה אחרת": הקלדה (2+ תווים) מפעילה debounce של 300ms שקורא ל-`GET /orders?search=` הקיים (כבר תומך בהתאמה גם לפי מספר הזמנה וגם לפי שם לקוח), מציג עד 8 תוצאות (הכי חדשות קודם) בתור שורות לחיצות — לחיצה על שורה מבצעת קישור מיידי (ללא צורך באישור נוסף). כפתור "קישור לפי מספר מדויק" נשמר כגיבוי ידני. מומש ב-`OrderDetail.jsx` + עיצוב `.link-search-results`/`.link-search-row` ב-`styles.css`.

## 11. מיקום פיזי + ברקוד — מומש ונבדק סופית (14.9.2026)

איתור בשני סבבים (טבלה ראשונה שזוהתה, `TDemoPritim`, התבררה כלא רלוונטית — לא הכילה את הפריטים האמיתיים של דניאל):

- **הטבלה הנכונה: `pritim`** — קטלוג הפריטים האמיתי, ממוין לפי `CompanyID`. אומת ישירות מול הזמנה אמיתית עם JBL FLIP 7 (14.9.2026): `stock_place="D02002"`, `barCode="1200130019302"` — תואם בדיוק למה שדניאל רואה בכרטיס הפריט בסיגמא עצמה.
- ה-JOIN הוא `pritim.prit_ID = azmanot.prit_ID AND pritim.CompanyID = azmanot.CompanyID` (לא רק לפי `prit_ID` — יש `CompanyID` בטבלה, אז משתמשים בו למניעת התנגשות בין חברות).
- ברקוד: כגיבוי (אם ריק בקטלוג), נופל בחזרה ל-`azmanot.FBarCode` שכבר קיים על שורת ההזמנה עצמה.

**מומש:**
- `bridge/sync.js` — שאילתת שורות ההזמנה: `LEFT JOIN pritim` לפי `prit_ID`+`CompanyID`, עם `NULLIF(LTRIM(RTRIM(...)), '')` להפוך מחרוזות ריקות ל-NULL אמיתי, ו-`COALESCE` לברקוד.
- `backend/src/schema.sql` / `db.js` — עמודת `order_items_cache.barcode` חדשה (מיקום כבר היה קיים).
- `backend/src/sigmaIngest.js` — `insertItem` מעדכן גם `barcode` (אותו `ON CONFLICT ... DO UPDATE` הזהיר שלא נוגע בשדות ליקוט/בדיקה פנימיים).
- `frontend/src/components/PickChecklist.jsx` — הברקוד מוצג בשורת ה-meta ליד מק"ט וכמות.
- **נבדק וסגור:** גם ברמת הקוד (קריאה ישירה ל-`ingestOrders`) וגם מול הדאטה האמיתי בסיגמא (שאילתות ממוקדות על prit_ID 18813/18815 — JBL FLIP 7 מהצילום מסך של דניאל). דניאל צריך להחליף את `sync.js` בפועל בתיקיית ה-Bridge שלו ולהפעיל מחדש את שירות Windows (`net stop`/`net start "Aladin Sigma Bridge"`).

### 11.1 תיקון נלווה: שורות שהושרשרו לחשבונית עדיין הוצגו למלקט

דיווח דניאל (הזמנה 192821, 14.9.2026): הזמנה עם שורה פתוחה אחת בלבד הציגה למלקט גם את כל שאר השורות שכבר שורשרו במלואן לחשבונית. הכלל `canceled=0 AND tquan>0` היה קיים כבר ברמת ההזמנה (ב-`fetchOpenOrders`, ר' סעיף למעלה על הזמנה 54464) אך חסר בשאילתת שורות הפריטים עצמה — תוקן באותו קובץ (`bridge/sync.js`).

### 11.2 תיקון נלווה: ניקוי שורות/הזמנות שנעלמו מסיגמא באמצע עבודה (14.9.2026)

הכלל בסעיף 11.1 פותר את הבעיה **קדימה** (Sigma לא שולחת יותר שורה שהוזמנה, אז לא נמשוך אותה), אבל לא ניקה שורות שכבר נשמרו אצלנו לפני התיקון. שני תיקונים ב-`backend/src/sigmaIngest.js`:

1. **`ingestOrders`** — בכל סבב, כל שורת פריט שהייתה קיימת ב-`order_items_cache` אבל לא הופיעה בסבב הנוכחי (כי שורשרה לחשבונית) נמחקת. כך המלקט תמיד רואה רק את מה שבאמת עדיין פתוח, ולא ילקט שוב פריטים שכבר טופלו.
2. **`reconcileOpenOrders`** — הורחב לטפל גם בהזמנות **שכבר בעבודה פעילה** (לא רק `waiting_pick`): אם הזמנה כזו נעלמת לגמרי מרשימת ה"פתוחות" של סיגמא (למשל שורשרה במלואה לחשבונית בזמן שהיא כבר בליקוט/בדיקה/אריזה), היא עוברת אוטומטית לסטטוס `on_hold` (עם `hold_reason` מסביר ו-`pre_wait_status` לשחזור), ומופיעה בפאנל **"הזמנות מעוכבות"** הקיים תחת חריגות — עד שמנהל בודק ומחליט (סוגר ידנית, או משחרר בחזרה עם `releaseHold`). **לא** נסגרת אוטומטית ו**לא** נשארת תקועה בתור הליקוט הפעיל בלי פריטים — בדיוק לפי בקשת דניאל.

נבדק end-to-end (ראה קומיט 357d01c): הזמנה → claim ל-picking → resync חלקי (2 מתוך 3 שורות נמחקות נכון) → ההזמנה נעלמת לגמרי → `reconcile` מעביר אותה ל-`on_hold`, לא לסגור ולא תקועה. גם נבדק על מדגם מעורב (הזמנות `waiting_pick` נסגרות, הזמנה `picking` עוברת ל-`on_hold`) — כל אחת מטופלת לפי הסטטוס שלה.

## 12. מסך המזכירה + תיקון בודק + חוסרי מלאי למנהל (מומש ונבדק 14.9.2026)

דניאל ביקש שלוש הרחבות אחרי שהמזכירה תתחיל להשתמש ביוזר `warehouse_manager`:

### 12.1 "היסטוריה" — ברור לגבי מה כבר הוצאה חשבונית ומה לא

- שני טאבים: **"דורש טיפול"** (ברירת מחדל — רק הזמנות עם חוסר שעוד לא סומן כטופל) ו-**"הכל"**.
- כרטיס עם חוסר לא-מטופל מודגש ויזואלית (מסגרת/רקע כתום עדין, `.shortage-needs-attention`).
- טבלת החוסרים כוללת עכשיו גם **הערת בודק** (`check_note`), לא רק הערת מלקט.
- כפתור **"✓ סימנתי שהוצאתי חשבונית מתוקנת"** ברמת ההזמנה כולה (לא per-שורה — הוחלט מול דניאל שבד"כ מוציאים חשבונית מתוקנת אחת per הזמנה) — שומר מי/מתי (`shortage_invoiced_at`/`by` על `workflow_state`), עם אפשרות "בטל סימון". זמין רק ל-`warehouse_manager`/`system_admin`.
- Endpoints: `POST /orders/:key/mark-shortage-invoiced`, `POST /orders/:key/unmark-shortage-invoiced` (`workflow.markShortageInvoiced`/`unmarkShortageInvoiced`).

### 12.2 הבודק יכול לתקן/לבטל את מה שהמלקט קבע

מלקט טעה (סימן "נלקט הכל" אבל בפועל חסר/כמות שונה, או להפך — סימן "חסר" אבל בעצם כן נמצא) — הבודק יכול לתקן בעצמו את השורה, לא רק לאשר/לדחות:

- פונקציה חדשה `workflow.correctPickedItem(orderKey, lineNo, userId, {qtyPicked, pickStatus, checkNote})` — זמינה רק בשלב `ready_for_check`, מקבלת כל pickStatus כולל הפיכת `missing`↔`picked`/`partial` (דו-כיווני). שומרת את `pick_note` המקורי של המלקט בלי לגעת בו, וכותבת את הסבר הבודק ל-`check_note` בנפרד — שקיפות מלאה על מי אמר מה. מסמנת `checked=1` אוטומטית (הבודק כבר קבע את השורה בעצמו).
- Endpoint: `POST /orders/:key/items/:lineNo/correct-pick`.
- `PickChecklist.jsx` (מצב `check`): כפתור "תיקון" זמין על **כל** שורה — גם שורה שסומנה "חסר" ("תיקון — בעצם כן נמצא"), גם שורה שכבר אושרה. פותח את אותו טופס כמות+הערה כמו במצב ליקוט.

### 12.3 "חוסרי מלאי" — עמוד נפרד למנהל מחסן

לא דוח per-הזמנה (זה תפקיד ה"היסטוריה" למזכירה) אלא **מרוכז לפי מק"ט** — כמה חסר בסה"כ וכמה הזמנות מושפעות, כדי לדעת מה להזמין מול ספק:

- `GET /inventory/shortages?days=1` (ברירת מחדל: היום) — מקבץ את כל שורות ה-`missing`/`partial` בטווח, לפי `item_code`, עם רשימת ההזמנות המושפעות ניתנת להרחבה.
- דורש עמודה חדשה `order_items_cache.pick_marked_at` (מתי `pick_status` סומן לאחרונה — ע"י מלקט או תיקון בודק) כדי לדעת מה "היום".
- עמוד חדש `frontend/src/pages/InventoryShortages.jsx`, טאב נפרד בתפריט התחתון (📉, זמין רק למנהל) — בכוונה **לא** בתוך "כלי ניהול" (זה נשאר הגדרות בלבד) וגם לא בתוך "היסטוריה" (קהל/מטרה שונים).
- **עודכן 14.9.2026** (בקשת דניאל, אחרי דיון על "שירות פרימיום ללקוחות"): שם הסוכן המשויך להזמנה מוצג בטבלת ההרחבה, וכפתור **"📤 חזר למלאי — עדכון ללקוח"** פותח WhatsApp (`wa.me`) עם הודעה מוכנה מראש — הסוכן בוחר בעצמו למי לשלוח (אין לנו טלפון לקוח שמור במערכת). **הוחלט במפורש לא לבנות אוטומציה מלאה** (זיהוי אוטומטי של חזרה למלאי + הודעה אוטומטית) — דורש מעקב מלאי בזמן אמת שלא קיים היום, ופרטי קשר לקוח; יישאר רעיון לפרויקט עתידי נפרד אם יתברר שנדרש. גרסה זו נותנת לסוכן בן-אדם לעדכן אישית — נחשב אפילו יותר "פרימיום" מהודעה אוטומטית.

**שני נוסחים שונים, לשני מסכים שונים** (דניאל הבהיר 14.9.2026 — חשוב לא לערבב):
- **מייד אחרי סיום ליקוט** (הכפתור הקיים "📤 שתף ללקוח" ב-`OrderDetail.jsx`, דרך `shareShortage.js`) — נוסח **מתנצל**, מודיע על חוסר שהתגלה עכשיו: *"שלום, מדברים ממחסן אלדין 🙏 בהזמנה מספר X התגלו N פריטים עם חוסר/כמות חלקית (פירוט מצורף). מתנצלים על אי הנוחות — נעדכן אתכם ברגע שהם יחזרו למלאי."*
- **ממסך "חוסרי מלאי"** (`InventoryShortages.jsx`, כשמוצר בפועל חוזר למלאי מאוחר יותר) — נוסח **שמח**, מבשר על חזרה למלאי: *"שלום, מדברים ממחסן אלדין! 🎉 בהזמנה מספר X הפריט Y (מק"ט Z) היה חסר — שמחים לעדכן שהוא חזר למלאי! רוצים שנשלח אותו אליכם?"*

**נבדק end-to-end בדפדפן מקומי:** הזמנה עם 2 שורות → מלקט מסמן אחת "נלקט הכל" ואחת "חסר" (בטעות) → בבדיקה, הבודק מתקן את השורה ה"חסרה" ל"נמצא חלקית (1 מתוך 2)" עם הערה → מאשר את שתי השורות → "היסטוריה" (כיוזר manager) מציגה את החוסר עם הערת הבודק בטאב "דורש טיפול" → לחיצה על "סימנתי שהוצאתי חשבונית" מעבירה אותה ל"הכל" עם חותמת מי/מתי ואפשרות ביטול → "חוסרי מלאי" מציג את הפריט (X-2, חסר 1, בהזמנה אחת) עם הרחבה למספר ההזמנה והלקוח.
