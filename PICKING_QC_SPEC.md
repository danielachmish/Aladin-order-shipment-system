# איפיון: ליקוט לפי מיקום + שלב בדיקה (QC) + דוח חוסרים למזכירה

סוכם עם דניאל, 14.9.2026. לא מומש עדיין — זה מסמך תכנון בלבד.

## 1. הבעיה והמטרה

היום ההזמנה נעלמת לתוך "ליקוט" כקופסה שחורה — מנוע הסטטוסים שלנו לא יודע כלום על מה קרה בפועל בתוך הליקוט (איזה פריט נמצא, איזה חסר), וברגע שהליקוט "הסתיים" אין בדיקה נוספת ואין דיווח מסודר על חוסרים למזכירה (היא צריכה לגלות את זה בעצמה כדי להוציא חשבונית נכונה).

המטרה: להוריד את המעקב לרמת **שורת פריט בודדת**, להוסיף **שלב בדיקה חובה** לפני אריזה, ולהפיק **דוח חוסרים ברור** (מק"ט + כמות) לסגירת חשבונית.

## 2. החלטות שסוכמו (לא לשנות בלי לחזור לדניאל)

| נושא | החלטה |
|---|---|
| מיקום פיזי | **קיים בסיגמא** — צריך רק לאתר את העמודה ולמשוך אותה דרך ה-Bridge |
| בדיקה (QC) | **חובה תמיד**, אי אפשר לדלג |
| מי בודק | **אין הגבלה טכנית** — יכול להיות אותו יוזר "מחסן" (יש login משותף אחד בפועל), הפרדת התפקידים היא הליך פנימי, לא נאכפת במערכת |
| דוח למזכירה | שורות חסרות בלבד, עם **מק"ט + כמות חסרה** |
| תיקון חשבונית | **ידני בשלב זה** — לא נשלח כלום בחזרה לסיגמא בשלב הזה |
| רעיון עתידי (לא עכשיו) | ייצוא קובץ TXT לסיגמא עם הכמויות בפועל, שיבצע UPDATE אוטומטי על ההזמנה. **דורש בירור נפרד** של פורמט הקובץ שסיגמא מצפה לו (יש למסמכי `06_erp_integration` תבנית import — צריך לבדוק אם יש מסמך מקביל ל"עדכון הזמנה קיימת", לא רק ליצירה) |

## 3. שינויי מודל נתונים (SQLite)

### `order_items_cache` — עמודות חדשות
```sql
ALTER TABLE order_items_cache ADD COLUMN qty_picked REAL;        -- כמות שנלקטה בפועל (NULL = עוד לא טופל)
ALTER TABLE order_items_cache ADD COLUMN pick_status TEXT;       -- 'picked' | 'partial' | 'missing'
ALTER TABLE order_items_cache ADD COLUMN pick_note TEXT;         -- הערת מלקט (אופציונלי, בעיקר לחוסר)
ALTER TABLE order_items_cache ADD COLUMN checked INTEGER DEFAULT 0;  -- 0/1 — האם הבודק אישר את השורה הזו
ALTER TABLE order_items_cache ADD COLUMN check_note TEXT;        -- הערת בודק אם תיקן משהו
-- location כבר קיימת כעמודה, כרגע תמיד NULL כי ה-Bridge לא ממלא אותה
```

### `workflow_events` — לא משתנה, ממשיך לתעד גם את המעברים החדשים כרגיל.

## 4. שינוי במנוע הסטטוסים

מוסיפים סטטוס חדש **`ready_for_check`** בין `picking` ל-`ready_to_pack`:

```
waiting_pick → picking → ready_for_check → ready_to_pack → waiting_pickup → delivered_to_ups/closed(איסוף עצמי) → closed
```

- `finishPicking` (הכפתור הקיים "סיום ליקוט") ישתנה: לפני שהוא מרשה לעבור הלאה, יבדוק שלכל שורות הפריט יש `pick_status` שאינו NULL (כל שורה טופלה — נלקטה/חלקית/חסרה). אם לא — שגיאה "יש שורות שלא טופלו".
- מעבר חדש: `POST /orders/:key/finish-check` — מ-`ready_for_check` ל-`ready_to_pack`. דורש שכל השורות עם `pick_status != 'missing'` יהיו `checked=1`. שורה שסומנה `missing` לא צריכה בדיקה (אין מה לבדוק).
- `OrderTimeline` (ציר הזמן בממשק) מקבל שלב נוסף: "בתור → ליקוט → בדיקה → אריזה → ...".

## 5. מסכי ממשק חדשים

### 5.1 מסך ליקוט משודרג (מחליף את מסך `picking` הקיים)
- שורות **ממוינות לפי `location`** (לא לפי `line_no` המקורי מסיגמא) — כדי שהמלקט ילך מסלול הגיוני.
- כל שורה: שם פריט, מק"ט, **תג מיקום גדול**, כמות מוזמנת.
- כפתור מהיר "✓ ליקטתי הכל" → `qty_picked = qty_ordered`, `pick_status='picked'`.
- אופציה לערוך כמות (חלקי) או לסמן "לא נמצא" (`pick_status='missing'`, אפשרות להוסיף הערה).
- מד התקדמות למעלה ("5/8 שורות טופלו").
- כפתור "סיום ליקוט" מופעל רק כשכל השורות טופלו → `finishPicking` → `ready_for_check`.
- **כפתור "שתף ללקוח" על שורה שסומנה `partial`/`missing` (חדש, סוכם 14.9.2026):**
  - בלחיצה, האפליקציה מציירת **תמונה** (canvas בצד הלקוח, לא צילום מסך) עם: שם פריט, מק"ט, "הוזמן X · יש במלאי Y · חסר Z", ומספר ההזמנה.
  - התמונה נשלחת לתפריט השיתוף המובנה של הטלפון (Web Share API עם קובץ) — כדי שהמשתמש יבחר WhatsApp/כל אפליקציה ויעביר ללקוח ישירות, בלי לצאת מהאפליקציה.
  - **גיבוי**: אם המכשיר/דפדפן לא תומך בשיתוף קבצים (חלק ממכשירי iOS ישנים) — הורדת התמונה במקום זאת, לשיתוף ידני מהגלריה.
  - לא דורש שינוי בבקאנד — כל הלוגיקה בצד הלקוח (יש כבר את כל הנתונים הדרושים ב-state של המסך).

### 5.2 מסך בדיקה (חדש, סטטוס `ready_for_check`)
- אותה רשימת שורות, מציג את מה שהמלקט דיווח (`qty_picked`, `pick_status`, `pick_note`).
- שורות `missing` מוצגות אך לא ניתנות לאישור (אין מה לבדוק בפריט שלא נמצא).
- לכל שורה אחרת: כפתור "✓ מאשר" (`checked=1`), או עריכה + הערה אם יש אי-התאמה (`check_note`).
- כפתור "אישרתי בדיקה — מוכן לאריזה" מופעל רק כשכל השורות הרלוונטיות `checked=1` → `finish-check` → `ready_to_pack`.

### 5.3 הרחבת "היסטוריה" — דוח חוסרים למזכירה
- בכל שורת הזמנה בהיסטוריה: אם יש שורת פריט עם `pick_status IN ('missing','partial')`, תג "⚠️ N פריטים חסרים".
- לחיצה על ההזמנה מציגה טבלה: מק"ט | שם פריט | כמות שהוזמנה | כמות שנלקטה | הערה.
- (אופציונלי בהמשך: מסך מרוכז "כל ההזמנות עם חוסרים" ברמת הארגון, לא רק per-order).

## 6. שינויים בבקאנד

- `backend/src/schema.sql` — 5 העמודות החדשות (סעיף 3).
- `backend/src/db.js` — `ensureColumn` migrations תואמות.
- `backend/src/workflow.js` — `ready_for_check` ב-`VALID_TRANSITIONS`, פונקציה `finishCheck`, ולידציה ב-`finishPicking`.
- `backend/src/routes.js` — endpoint חדש לעדכון שורת פריט בודדת (`POST /orders/:key/items/:lineNo/pick`, `POST /orders/:key/items/:lineNo/check`), ו-`POST /orders/:key/finish-check`.
- `backend/src/routes.js` (`/history`) — כולל את שורות הפריט עם `pick_status` כדי שהממשק יבנה את תג/טבלת החוסרים.

## 7. שינויים ב-Bridge (`bridge/sync.js`)

**שלב מקדים — עדיין לא בוצע:** להריץ סקריפט אבחון (כמו `inspect-schema.js` הקודמים) מול טבלת `azmanot` כדי לזהות באיזו עמודה שוכן המיקום הפיזי. לאחר זיהוי:
- להוסיף את העמודה לשאילתת שורות הפריט ב-`fetchOpenOrders()`.
- להעביר את הערך דרך `ingestOrders` אל `order_items_cache.location`.

## 8. תוכנית עבודה מוצעת (סדר בנייה)

| שלב | מה | תלוי ב-|
|---|---|---|
| 1 | סקריפט אבחון ל-Bridge — למצוא עמודת מיקום בסיגמא | גישה למחשב העבודה שלך |
| 2 | עדכון `sync.js` למשוך ולשלוח מיקום | שלב 1 |
| 3 | מיגרציית סכמה (5 עמודות חדשות) + `ensureColumn` | — |
| 4 | הרחבת מנוע הסטטוסים (`ready_for_check`, `finishCheck`) + endpoints לשורת פריט | שלב 3 |
| 5 | מסך ליקוט משודרג (מיון לפי מיקום, סימון per-line) | שלב 4 |
| 6 | מסך בדיקה חדש | שלב 4 |
| 7 | הרחבת ציר הזמן (OrderTimeline) לשלב הבדיקה | שלב 4 |
| 8 | הרחבת היסטוריה — תג + טבלת חוסרים | שלב 4 |
| 9 | בדיקה מקצה לקצה עם הזמנה אמיתית שיש בה חוסר מכוון | הכל |

**הערכת היקף:** בסדר גודל דומה (אולי מעט יותר גדול) מכל מה שנבנה עד כה לתור הליקוט הבסיסי — בעיקר בגלל המעקב ברמת שורה (לא רק רמת הזמנה) שדורש שינוי בכל המסכים שנוגעים בפריטים.

## 9. שאלות פתוחות להמשך (לא חוסמות את הבנייה הנוכחית)

- פורמט קובץ TXT עתידי לעדכון סיגמא (סעיף 2) — צריך מסמך spec נפרד מסיגמא/מהאפיון המקורי לפני שנתכנן את זה.
- האם דוח החוסרים צריך להיות "בר-ייצוא" (PDF/Excel) למזכירה, או מסך צפייה בתוך האפליקציה מספיק בשלב זה?
