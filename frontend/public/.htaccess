# NOTE: on this Cloudways server, .php requests are routed by Nginx straight
# to PHP-FPM, bypassing Apache/.htaccess entirely -- so mod_rewrite/mod_proxy
# directives here have no effect on /api.php requests (verified empirically).
# The actual API reverse-proxy lives in api.php itself (PHP cURL), not here.
Options -Indexes
