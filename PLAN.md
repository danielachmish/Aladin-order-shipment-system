# תוכנית עבודה – בניית מערכת אלדין מקצה לקצה

מבוסס על `Aladin_Order_and_Shipment_System_Specification_HE` גרסה 1.2.
מטרה: MVP עובד שמדגים את כל התהליך המרכזי מקומית (localhost), עם ממשקי חיבור
ברורים כך שאפשר יהיה בהמשך לחבר Sigma אמיתי ו-UPS אמיתי בלי לשנות את הליבה.

## החלטות טכניות (עם הצדקה)

| נושא | החלטה | למה |
|---|---|---|
| מסד נתונים (dev) | SQLite (better-sqlite3) | אפס התקנה, ריצה מיידית. הסכמה נבנית קרוב ל-Postgres כדי שהמעבר יהיה פשוט. Docker לא זמין/לא רץ בסביבה כרגע. |
| מסד נתונים (prod יעד) | PostgreSQL, כמו שמצוין בסעיף 7 באפיון | ללא שינוי מהאפיון |
| Backend | Node.js + Express + ws (WebSocket) | קל, תואם לדרישת ה-Realtime, לא תלוי בענן חיצוני |
| Auth | JWT פשוט, משתמשים seed לפי תפקיד (מנהל/סוכן/מחסן) | תואם סעיף 2 |
| Sigma Bridge | **Mock**: מודול שמזין orders_cache בנתוני דמו (כולל הזמנה 54707) במקום התחברות אמיתית ל-SQL Server | אין גישה לרשת Sigma האמיתית מהסביבה הזו; הממשק (interface) זהה כך שאפשר להחליף למימוש אמיתי בקובץ אחד |
| UPS | Webhook endpoint אמיתי לפי הפורמט מהאפיון (trackNo, ref1 מופרד פסיק וכו') + סקריפט לדימוי אירועי UPS לבדיקה | אין credentials אמיתיים ל-UPS; הלוגיקה של הקישור האוטומטי (ref1 -> הזמנות) ממומשת באמת ונבדקת |
| Frontend | React + Vite, PWA (manifest + service worker), עברית RTL, מובייל-פירסט 360-430px | תואם סעיף 3 |

כל מקום שבו יש mock מסומן בקוד בהערה `// MOCK:` ומתועד ב-README.

## שלבי עבודה (Todo)

0. תשתית ריפו: מבנה תיקיות, package.json, README, .gitignore
1. סכמת מסד נתונים מלאה (10 טבלאות מסעיף 10) + seed (משתמשים, הזמנת בדיקה 54707 + עוד כמה)
2. Backend – שכבת נתונים + מנוע הסטטוסים (workflow) כולל: נעילה אטומית (claim), מספר גרסה (optimistic concurrency), יומן אירועים
3. Backend – API לפי סעיף 11 (orders, order details, status, claim, issue, admin/shipment-links, webhooks/ups, shipments refresh, live, health)
4. Backend – מנוע התור (queue) לפי סעיף 5: תור נפרד לכל תחנה, עדיפות/דחיפות, "הבא בתור", מיקום בתור
5. Backend – בקשת דחיפות מסוכן + אישור מנהל (workflow משלו + היסטוריה)
6. Backend – סטטוס "ממתינה לתשובת לקוח/סוכן" (חזרה לשלב קודם בדיוק)
7. Backend – UPS webhook: פענוח ref1, קישור many-to-many, מניעת כפילות אירועים, חריגת קישור למנהל
8. Backend – Realtime (WebSocket) לכל שינוי הזמנה/תור/משלוח
9. Backend – הרשאות: מלקט/אורז/סוכן/מנהל מחסן/מנהל מערכת + מתג "תצוגת הזמנות לסוכנים"
10. Frontend – שלד PWA (מניפסט, service worker, כניסה RTL)
11. Frontend – מסך הזמנות ותור (חיפוש, מדדי-על, כרטיסים, live)
12. Frontend – מסך פרטי הזמנה (פעולה לפי שלב, דיווח בעיה, ממתין לתשובה, כרטיס משלוח)
13. Frontend – מסך חריגות
14. Frontend – מסך ניהול (מתג תצוגת סוכנים, אישור/דחיית דחיפות, קביעת "הבא בתור")
15. Frontend – תצוגת סוכן (מקום בתור, בקשת דחיפות)
16. בדיקות קבלה: תרחיש הזמנה 54707 מלא (ליקוט על 2 "מכשירים" = 2 טאבים, מניעת claim כפול, webhook עם הזמנה יחידה ורשימה, סטטוס ממתין לתשובה)
17. README סופי + עדכון לדניאל

## קריטריון סיום MVP
- אפשר להריץ `npm run dev` ב-backend וב-frontend, להיכנס כ-3 סוגי משתמשים, ולבצע את כל תרחיש הזמנה 54707 (סעיף 16.1) מקצה לקצה בדפדפן, כולל עדכון חי בין שני טאבים.
- כל מה שממומש כ-mock (Sigma, UPS אמיתי) מתועד בבירור.

---
*קובץ זה מתעדכן תוך כדי עבודה. סטטוס נוכחי בתחתית.*

## סטטוס
- [x] שלב 0 – תשתית ריפו
- [x] שלב 1 – סכמת מסד נתונים + seed (54707 ועוד 3 הזמנות)
- [x] שלב 2 – מנוע workflow (claim אטומי, גרסאות, יומן אירועים)
- [x] שלב 3 – API מלא
- [x] שלב 4 – מנוע התור (עדיפות/הבא בתור/מקום בתור)
- [x] שלב 5 – בקשת דחיפות + אישור מנהל
- [x] שלב 6 – סטטוס ממתינה לתשובת לקוח/סוכן
- [x] שלב 7 – UPS webhook (קישור רבים-לרבים, דדופ, חריגת קישור)
- [x] שלב 8 – Realtime (WebSocket)
- [x] שלב 9 – הרשאות + מתג תצוגת סוכנים
- [x] בדיקה ידנית מלאה של כל תרחיש 16.1 מול ה-API בפועל (claim כפול נחסם, webhook עם 2 הזמנות, אסמכתא חלקית שגויה, דחיפות מאושרת מזיזה בתור) — עבר בהצלחה. פורט: 4310 (4000 שמור ע"י Windows).
- [x] שלב 10 – שלד PWA (manifest, service worker, כניסה RTL)
- [x] שלב 11 – מסך הזמנות ותור (חיפוש, מדדי-על, כרטיסים, live)
- [x] שלב 12 – מסך פרטי הזמנה (פעולה לפי שלב, דיווח בעיה, ממתין לתשובה, כרטיס משלוח, בקשת דחיפות)
- [x] שלב 13 – מסך חריגות
- [x] שלב 14 – מסך ניהול (מתג תצוגת סוכנים, אישור/דחיית דחיפות)
- [x] שלב 15 – תצוגת סוכן (מקום בתור, בקשת דחיפות, הגבלת נראות)
- [x] שלב 16 – בדיקות קבלה: הרצתי את כל תרחיש 54707 (סעיף 16.1) בפועל בדפדפן עם
      3 משתמשים בו-זמנית (מחסן/מנהלת/סוכן) בשלושה טאבים: claim חוסם claim כפול,
      webhook עם אסמכתא לשתי הזמנות מקשר trackNo אחד לשתיהן ומעדכן חי, אסמכתא
      חלקית שגויה יוצרת חריגת מנהל בלי לעצור את המחסן, אישור דחיפות מזיז הזמנה
      לראש התור ומשודר Live לסוכן תוך פחות משנייה, מתג "תצוגת הזמנות לסוכנים"
      עובד ומיקום בתור נשאר אמיתי (מול התור הגלובלי) גם במצב "רק שלי".
- [x] שלב 17 – README.md סופי + עדכון לדניאל

## הכל הושלם ✅
MVP עובד מקצה לקצה, רץ מקומית, נבדק ידנית מול כל תרחישי הקבלה בסעיף 16 באפיון.
